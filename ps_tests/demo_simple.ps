// Simple PowerScript Demo
class Calculator {
    public add(a: number, b: number): number {
        return a + b;
    }
    
    public multiply(x: number, y: number): number {
        return x * y;
    }
}

let calc = new Calculator();
let result = calc.add(5, 3);
console.log("Result: " + result);