import * as asyncio from "asyncio";

async function async_add(a: number, b: number): number {
    await asyncio.sleep(0.1);
    return a + b;
}

function main() {
    let result = asyncio.run(async_add(5, 3));
    console.log(result);
}

main();