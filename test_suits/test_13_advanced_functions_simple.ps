// Test 13: Advanced Functions (Simplified)
// Tests: recursion, higher-order functions

console.log("=== Test 13: Advanced Functions ===");

// Higher-order functions
function applyOperation(x, y, operation) {
    return operation(x, y);
}

let add = (a, b) => a + b;
let multiply = (a, b) => a * b;

console.log("Apply add:", applyOperation(5, 3, add));
console.log("Apply multiply:", applyOperation(5, 3, multiply));

// Map simulation
function mapArray(arr, fn) {
    let result = [];
    for (item in arr) {
        result.push(fn(item));
    }
    return result;
}

let numbers = [1, 2, 3, 4, 5];
let squared = mapArray(numbers, x => x * x);
console.log("Squared:", squared);

// Filter simulation
function filterArray(arr, predicate) {
    let result = [];
    for (item in arr) {
        if (predicate(item)) {
            result.push(item);
        }
    }
    return result;
}

let evens = filterArray(numbers, x => x % 2 == 0);
console.log("Even numbers:", evens);

// Reduce simulation
function reduceArray(arr, fn, initial) {
    let accumulator = initial;
    for (item in arr) {
        accumulator = fn(accumulator, item);
    }
    return accumulator;
}

let sum = reduceArray(numbers, (acc, x) => acc + x, 0);
console.log("Sum:", sum);

// Recursive functions
function fibonacci(n) {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log("Fibonacci(10):", fibonacci(10));

console.log("✅ Test 13 Passed: Advanced Functions");
